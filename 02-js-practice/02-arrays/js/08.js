/**
 **Write a function that takes an array of strings as argument. Sort the array elements alphabetically. Return the result.
 */

const myFunc = (a) => {
  return a.sort();
};

const arr = ['grapes', 'strawberries', 'banana', 'apple'];
console.log(myFunc(arr));
