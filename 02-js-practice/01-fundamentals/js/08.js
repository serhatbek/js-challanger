/**
 ** Write a function that takes a string as argument. The string contains the substring 'is'. Return the index of 'is'.
 */

const myFunc = (str) => {
  return str.indexOf('is');
};

console.log(myFunc('Daisy'));
