/**
 ** Write a function that takes a string as argument. Extract the last 3 characters from the string. Return the result
 */

const myFunc = (str) => {
  return str.slice(-3);
};

console.log(myFunc('Banana Prince'));
