/**
 ** In this exercise, we pass two arguments to the function multiply. multiply receives these arguments via the parameters x and y.
 ** Adjust the code below so that the console.log() statement logs true.
 */

const multiply = (x, y) => {
  return x * y;
};

const arg1 = 3,
  arg2 = 4;

const result = multiply(arg1, arg2);

console.log('result 12', result === 12);
